fn List<int> main() {
    Dictionary<String, int> var_dict = |
    "dog": 3,
    "cat": 4,
    "cow": 5,
    "hamster": 6
     |;

     List<int> list =  SELECT (var_dict.value) FROM var_dict;

     Dictionary<int, String> dict = SELECT (var_dict.value, var_dict.key) FROM var_dict;

     List<String> list2 =  SELECT (var_dict.key) FROM var_dict WHERE (var_dict.value > 3);

     List<int> list3 = SELECT (var_dict.value) FROM var_dict WHERE (var_dict.key == "dog");

     List<String> list4 = SELECT (var_dict.key) FROM var_dict WHERE (var_dict.key == "dog");

     List<String> list5 = SELECT (var_dict.key) FROM var_dict WHERE (var_dict.key != "dog");

     //dict.sort((Tuple<String, int> a,Tuple<String, int> b) => a.get(1) > b.get(1));


     return SELECT (var_dict.value) FROM var_dict WHERE (var_dict.value <= 4);
}