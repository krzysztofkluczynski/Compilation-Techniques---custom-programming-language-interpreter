fn int sum(List<int> list) {
    //one line comment
    int sum = 0;
    for (int i : list) {
        print("Value in list: " + $String i);
        sum = sum + i;
    }
    return sum;
 }


 fn int main() {
    /*
    multi line comment
    */

    Dictionary<String, int> var_dict = |
    "dog": 0,
    "cat": 2,
    "cow": 3,
    "hamster": 1
     |;

     String test = "test";
     test = "new test";
     print(test);

     List<int> list;
     list = SELECT (var_dict.value) FROM var_dict WHERE (var_dict.key != "cat");

     int x = sum(list, 2);
     print("Sum of list without cat value: " + $String x);

     list.set(0, 5);
     String s = "List value at index 0 after change: " + $String list.get(0);
     print(s);


    return -x + 1 * 3;
 }